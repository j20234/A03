# 核心依赖
numpy>=1.21.0
pandas>=1.5.0

# 数据源
yfinance>=0.2.0
tushare>=1.2.0

# 可视化
matplotlib>=3.5.0

# 测试
pytest>=7.0.0

# 性能优化（可选但强烈推荐）
numba>=0.56.0  # JIT编译加速，提供10-100倍性能提升
psutil>=5.9.0  # 系统和进程监控

# 异步支持
asyncio>=3.4.3

# 数据类型优化
pyarrow>=10.0.0  # 高效的列式存储格式（可选）

# 并行处理（可选）
# joblib>=1.2.0
# dask>=2023.1.0

# 数据库支持（可选）
# sqlalchemy>=2.0.0
# redis>=4.5.0
